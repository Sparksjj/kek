import { CustomQuillDirective } from './custom-quill.directive';

describe('CustomQuillDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomQuillDirective();
    expect(directive).toBeTruthy();
  });
});
